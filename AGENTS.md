完成代码修改后，请检查本次改动是否适合提交，并按照 Conventional Commits 风格生成中文提交信息。

要求：
- 类型使用 feat / fix / refactor / style / docs / test / chore 等。
- 提交标题使用中文，简洁明确。
- 格式为：<type>: <中文描述>
- 如果有必要，可补充多行中文说明本次改动目的。

推送远程前必须先检查远程工作流是否会通过。

要求：
- 后端改动或合并远程分支后，必须在 `backend` 目录运行远程 CI 同款命令：`uv run python -m pytest`。
- 如果本地环境无法直接运行 CI 同款命令，必须明确说明原因，并至少运行等价的全量测试命令后再请求确认。
- 不要只依赖受影响测试、编译检查或 pre-push hook；推送前必须主动确认全量工作流级检查。
