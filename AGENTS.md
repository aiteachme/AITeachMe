完成代码修改后，请检查本次改动是否适合提交，并按照 Conventional Commits 风格生成中文提交信息。

要求：
- 类型使用 feat / fix / refactor / style / docs / test / chore 等。
- 提交标题使用中文，简洁明确。
- 格式为：<type>: <中文描述>
- 如果有必要，可补充多行中文说明本次改动目的。

推送远程前，请主动想一遍远程工作流是否也会过。

建议：
- 后端改动、合并远程分支，或改到测试收集、依赖、生成物时，优先在 `backend` 目录跑 CI 同款命令：`uv run python -m pytest`。
- 如果本地环境和 CI 不完全一致，比如没有 `uv` 命令、平台差异、临时目录权限问题等，可以说明原因，并跑尽量等价的全量检查，例如 `python -m uv run python -m pytest`。
- 受影响测试、编译检查和 pre-push hook 都有用，但它们更适合作为补充。若这次判断不需要全量检查，需要把判断依据和剩余风险说清楚。
