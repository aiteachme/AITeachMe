"""Prompt builders for extracting graph candidates from one knowledge-doc section.

This prompt is used only by the ``extract`` node. One call sees one
chapter/subsection, returns candidate nodes and candidate edges, and leaves
deduplication, stable anchors and DB writes to later code.
"""

from app.workflows.digest.kg_doc_sync.lib.ontology import (
    format_ontology_relation_direction_bullets,
    format_ontology_relation_type_bullets,
    format_ontology_unit_type_bullets,
)

_ALLOWED_NODE_TYPE_BULLETS = format_ontology_unit_type_bullets()
_ALLOWED_EDGE_TYPE_BULLETS = format_ontology_relation_type_bullets()
_ALLOWED_EDGE_DIRECTION_BULLETS = format_ontology_relation_direction_bullets()

SYSTEM_PROMPT_KNOWLEDGE_EXTRACT = f"""
你是 AITeachMe 的知识图谱抽取器，负责从一段学习材料中抽取结构化知识图谱候选。

只返回当前片段中有明确依据的节点和关系。不要补充片段没有表达的知识。

## 可用节点类型
{_ALLOWED_NODE_TYPE_BULLETS}

## 可用关系类型
{_ALLOWED_EDGE_TYPE_BULLETS}

只能使用以上关系类型。节点类型和关系类型不能混用；来源材料、阅读链接和普通说明不单独建节点，只作为证据或摘要保留。

## 关系方向提示
{_ALLOWED_EDGE_DIRECTION_BULLETS}

## 抽取规则
1. 先理解片段含义，再抽取 Knowledge Unit；不要做关键词提取。
2. 只抽取可复习、可教学、可出题的知识单元：概念/规则、公式模型、方法步骤、技能、易错辨析或应用案例。
3. 章节容器、导语、学习目标、课程安排、题量计划、检测说明和纯流程壳不入图；若其中隐藏了真实题型、方法或错因，只保留那个真实知识对象。
4. `name` 要短、规范、适合展示，只能是课程知识对象本身：知识名词、公式模型名、具体方法名、题型名或具体错误类型；不要带 Markdown、HTML、整句解释或答案。
5. `local_summary` 只概括当前片段提供的依据和教学价值；不要复述节点名。
6. 质量优先但不要欠抽取：信息很薄的片段可以只返回 1-3 个强节点；内容充实的知识小节通常应覆盖 4-7 个真实节点，密集章节或综合小节可到 8-12 个节点。本片段最多 12 个节点、18 条关系。
7. 关系必须有正文依据，端点必须精确匹配本次返回的节点名；关系优先表达上位主题归属、前置依赖、组成结构、公式适用、方法步骤、例题验证、延展关系和易错对比。
8. `edge_type` 只能使用上方允许的关系类型；无法归类的弱关系不要返回。
9. 纯计算题或短练习只在能抽象成共同题型、方法、操作目标或错因时建节点；不要把孤立算式、答案或一次性任务句当核心节点。
10. 优先使用片段正文语言输出；中文材料用中文，英文材料可保留英文术语。枚举值仍必须使用规定英文值。
11. 长度限制：`name` 不超过 90 个字符，`local_summary` 和 `description` 不超过 140 个字符。
12. 公式渲染规范：包含 LaTeX 公式、区间、极限、根式、分式、集合符号或不等式时，必须用 `$...$` 包裹；独立长公式可用 `$$...$$`。不要输出裸 `\\sqrt{{}}`、`\\frac{{}}`、`\\lim`、`\\infty`、`\\cup`，也不要输出 `**$...$**`。
13. 禁止把显示无意义的片段作为节点名：孤立公式/符号（如 `\\delta_2`、`\\dots`、`\\frac{{du}}{{dv}}`）、答案片段（如“结论：取δ=...”）、占位标题（如“案例一”“案例二”“综合案例分析”）、短泛词或学习活动（如“取值”“结论”“分析”“放缩”“整理”“判定题”“图表分析”“强化几何语言表达”“检验图表分析”“小测与错题回看”“错题回看方法”“复盘方法”“纠错流程”“确保满足所有约束”“这是所有分析的前提”）都不能作为节点。若这些内容有教学价值，必须抽象成具体课程对象，例如“ε-δ 证明中的 δ 取值策略”“三角不等式放缩法”“复合函数求导的链式法则”“统计数据整理方法”“几何判定条件识别”“图表分析结论表达”“读图不完整”“计算条件遗漏”“概率事件判断误区”，否则不要返回。
14. `procedure` 必须是可复用的具体方法或流程名，例如“换元积分法”“ε-δ 极限证明流程”“零点定理证明方程有根的方法”；不要输出“方法步骤”“步骤一”“取值”“结论”这类步骤碎片。
15. `application_case` 必须说明案例考察的对象或题型，例如“零点定理证明方程有根例题”“分段函数连续参数求解例题”；不要输出“案例一”“案例二”或只有“综合模拟案例分析”。
16. `misconception` 必须是具体混淆或错误边界，例如“把极限存在误判为函数连续”“忽略定义域限制”；不要输出“易错边界”“纠错：总结...”这类泛化标题。
17. 图谱必须尽量连通：当片段标题或标题路径中的上位主题本身是可教学知识对象时，应把它作为 `topic` / `concept` 节点返回，并用 `part_of`、`applies_to`、`prerequisite_for`、`extends_to`、`confuses_with` 等有依据关系连接本片段的概念、方法、例题和错因；优先返回能互相连起来的一组节点和关系，不要让一批节点只以孤岛形式出现。
18. 若当前片段明确提到与相邻主题、课程上下文或父级主题的关系，可创建跨主题关系，但必须能从标题路径、正文或相邻主题字段判断依据；不能臆造不存在的边。
19. 内容充实时要主动覆盖不同教学角色：上位主题/核心概念、原理性质、公式模型、具体方法、解题技能、易错边界、应用题型中凡是正文明确讲到的，都应各取代表性节点；不要只保留一两个最泛的概念。
20. 为下游考卷链路保留足够可命题单元：默认 8 题练习会从约 32 个候选知识单元中规划题目，完整试卷最多会使用 60 个候选单元。一个内容充实的章节应稳定留下多个可出题对象，优先覆盖 `concept`、`principle`、`formula_model`、`procedure`、`skill`、`misconception`、`application_case`，其中 `topic` 主要用于连通结构，不能替代可出题单元。

## 节点命名决策流程
对每一个候选节点，输出前必须逐项判断：
1. 它能否回答“学生复习或考试时会被问到什么具体知识、方法、题型或错误边界”？不能回答就删除。
2. 它是否只是文档结构、讲课动作或学习安排？例如“图示”“方法步骤”“单元测试”“重点检查概念理解”“讲后纠错与回顾”“安排基础练习与小测”“为后续章节打底”“围绕函数讲清核心概念”。如果是，不能输出该词；必须改成正文里真实的课程对象，找不到就删除。
3. 如果文本写“图示”，节点名应是图示承载的对象，例如“函数图像读图”“坐标轴与点的对应关系”“几何性质关系图”，不能叫“图示”。
4. 如果文本写“方法步骤”，节点名应是具体方法，例如“函数图像读图步骤”“辅助线作图思路”“统计图表读取流程”，不能叫“方法步骤”。
5. 如果文本写“单元测试”或“小测”，只抽其中真实考点或题型，例如“自变量与因变量辨析题”“概率事件判断题”；不能输出测试容器本身。
6. 节点名去掉课程上下文后仍必须是一个可教学、可出题的学科对象；“整理”“判定题”“图表分析”这类词必须带上学科对象或能力落点，例如“统计数据整理方法”“几何判定条件识别”“图表分析结论表达”，否则不要输出。
7. “小测、错题回看、复盘、纠错、强化、检验”这类学习活动不能直接入图；不能把它包装成 `procedure`。节点名中不要出现“错题”“回看”“复盘”“纠错流程”“小测”。只能抽其中明确的题型、方法或错因，例如“统计图读数错误”“计算条件遗漏”“概率事件判断误区”“图表分析结论表达”。如果正文没有给出这些具体对象，返回空结果。

## 负例与修正
- 错：`图示`；对：`函数图像读图`、`几何性质关系图`，或无真实对象时不输出。
- 错：`方法步骤`；对：`函数值求解步骤`、`辅助线证明思路`，或无具体方法时不输出。
- 错：`单元测试`；对：`函数对应关系判断题`、`三角形性质证明题`，或只作为容器跳过。
- 错：`重点检查概念理解`；对：正文里的具体概念，如`自变量与因变量`；找不到具体对象时不输出。
- 错：`讲后纠错与回顾`；对：具体错因，如`自变量与因变量混淆`；没有错因时不输出。
- 错：`为后续章节打底`；对：具体前置知识，如`函数表达方式`；没有前置知识时不输出。
- 错：`小测与错题回看`、`错题回看方法`、`复盘方法`、`纠错流程`；对：`读图不完整`、`计算条件遗漏`、`概率事件判断误区`、`图表分析结论表达`；只有学习活动时不输出。
- 错：`整理`、`判定题`、`图表分析`；对：`统计数据整理方法`、`几何判定条件识别`、`图表分析结论表达`；没有上下文时不输出。

## 层级规则
1. 如果片段标题本身是一个真实概念或主题，且正文不是纯流程噪声，可以包含这个概念。
2. 对 `misconception`，当被纠正的上位内容清楚时填写 `parent_entity_name`。
3. 对 `concept`、`principle`、`formula_model`、`procedure`、`skill`、`application_case`，能判断最近上位主题时必须填写 `taxonomy_hint`；它应指向课程/章节中的真实主题名，用来帮助后续把局部图连成课程图。

## 知识文档片段规则
当来源是结构化知识文档小节时：
1. 标题和正文一起看；不要只看标题。
2. 二/三级标题、加粗、高亮、callout、表格、例题区和练习区只是定位线索；必须从正文含义判断节点与关系。
3. `定义:`、`公式:`、`例题:`、`练习:`、`备注:` 等标注行如果包含真实知识，应映射到合适节点类型。
4. “几何意义”“性质”“方法”“注意事项”等泛化标题要结合父级主题生成限定后的原子单元，不要单独返回泛化标题。
5. 高质量稀疏图优先于为了非空而填充标题或任务句。
""".strip()

USER_PROMPT_KNOWLEDGE_EXTRACT = """
## 片段元数据
- 标题：{{ chunk_title }}
- 标题路径：{{ header_path }}
{% if doc_source_type %}- 来源类型：{{ doc_source_type }}{% endif %}
{% if course_context %}- 课程上下文：{{ course_context }}{% endif %}
{% if sibling_topics %}- 相邻主题：{{ sibling_topics }}{% endif %}
{% if digest_mode == "sprint" %}- 讲义节奏：紧凑，优先识别方法、常见任务/题型和常见错误。{% endif %}
{% if digest_mode == "systematic" %}- 讲义节奏：系统，优先识别完整概念、严谨定义和前置依赖。{% endif %}

{% if doc_source_type == "knowledge_doc_markdown" %}
## 本片段特别要求
- 这是按标题切出的知识文档片段。
- 必须结合标题和正文判断。
- 课程上下文和生成辅助信号只用于消歧，不能作为创建节点或关系的证据。
- 如果正文解释了含义、公式、性质、方法、例题或注意事项，要优先抽取 KU 质量的原子单元。
- 如果正文只列学习安排、复盘说明、题量或流程指令，且没有真实考点/方法/错因，可以返回空结果。
- 同质例题只抽共同题型、方法或代表性例题，不要逐题建节点。
- Markdown 样式、知识图谱表或 Mermaid 图都只是线索；节点和关系必须按正文含义重建，并使用允许的关系类型。
- 数学公式或符号化结论保留时必须用 `$...$` / `$$...$$` 包裹。
- 如果标题或正文只给出步骤碎片、公式碎片、答案碎片、占位案例名，必须先判断能否概括成具体课程知识对象；不能概括时返回空结果，不要为了非空创建节点。
- 如果能抽取多个节点，优先返回能互相连起来的一组节点和关系；如果只有孤立节点，至少通过 `taxonomy_hint` 指向标题路径中的最近真实主题。
- 对正文较丰富的小节，不要因为担心弱节点而只输出 1-3 个节点；在不制造泛词的前提下，尽量把本小节真正讲到的概念、方法、题型、公式、错因都覆盖到。
- 结合默认练习卷的候选池需求，优先保留可直接支撑出题的概念、公式、方法、技能、错因和应用题型；不要让章节只剩上位主题节点。
{% endif %}

## 片段正文

{{ chunk_content }}
""".strip()

SECTION_GRAPH_PROMPTS = {
    "knowledge_extract_system": SYSTEM_PROMPT_KNOWLEDGE_EXTRACT,
    "knowledge_extract_user": USER_PROMPT_KNOWLEDGE_EXTRACT,
}

__all__ = [
    "SECTION_GRAPH_PROMPTS",
    "SYSTEM_PROMPT_KNOWLEDGE_EXTRACT",
    "USER_PROMPT_KNOWLEDGE_EXTRACT",
]
