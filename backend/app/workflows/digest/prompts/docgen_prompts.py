"""Prompt templates for digest docs generation."""

from __future__ import annotations


CLEANSE_PROMPT = """\
你是一个文本修复助手。你的任务只有一个：修复下面文本里明显的 OCR 错字、乱码和 PDF 提取噪声。

要求：
1. 只修复明显错误，不重写结构，不补充内容。
2. 不做总结，不改写，不删减信息。
3. 如果原文已经足够干净，原样返回。

输入文本：
{text}

输出要求：
直接返回修复后的正文，不要添加解释。
"""


LOCAL_OUTLINE_PROMPT = """\
你是一个教学大纲整理助手。请根据下面文本，提取 3-5 个能概括主要知识点的子标题。

要求：
1. 每个标题不超过 20 字。
2. 按内容出现顺序排列。
3. 只返回 JSON 数组，不要解释。

文本内容：
{text}

输出示例：
["标题一", "标题二", "标题三"]
"""


GLOBAL_OUTLINE_PROMPT = """\
你是一位经验丰富的教研老师。请把下面零散的知识点整理成适合学习和复习的多章节结构。

学科信息：
{subject_context}

输入：
- 原始文本块数量：{chunk_count}
- 局部标题汇总：
{local_outlines}

用户补充要求：
{user_prompt}

要求：
1. 输出两级结构：章 -> 节。
2. 章节顺序要符合该学科的教学逻辑，基础在前，综合应用在后。
3. 每一节都要标明对应的原始文本块索引。
4. 不要漏掉重要主题，也不要制造虚假章节。
5. 章节标题要体现学科特色，使用该学科的专业术语。

输出格式：
返回严格 JSON：
{{
  "chapters": [
    {{
      "chapter_index": 1,
      "title": "章标题",
      "sections": [
        {{
          "title": "节标题",
          "source_chunk_indices": [0, 1]
        }}
      ]
    }}
  ]
}}
"""


WRITER_PROMPT = """\
你是 AITeachMe 的金牌私教。请基于分配给你的原始素材，写出一章真正适合学习和复习的中文讲义。

学科信息：
{subject_context}

写作风格指导：
{teaching_style_hint}

全局大纲：
{global_outline}

当前章节：
- 标题：{chapter_title}
- 序号：第 {chapter_index} 章 / 共 {total_chapters} 章
- 本章重点小节：{section_titles}

用户补充要求：
{user_prompt}

上一章摘要：
{prev_summary}

本章素材导读：
{source_brief}

本章关键公式参考：
{formula_refs}

本章原始素材：
{source_content}

下一章预告：
{next_preview}

写作要求：
1. 只输出这一章，不要写整本书。
2. 以 `# {chapter_title}` 作为唯一一级标题。
3. 开头必须有 `> 📌 本章概要：...`，用 2-3 句话说明本章主线。
4. 正文要写成自然的讲义，不要写成拼接摘要，也不要空洞列点。
5. 使用自然的二级、三级标题组织内容，讲清概念、公式、推导思路、例题和易错点。
6. 公式请保留 LaTeX 写法，并确保符号准确。
7. 不要照搬原文，要用教学化语言重写，但不能遗漏关键概念和公式。
8. 文末附一行：`📊 本章标签：#标签1 #标签2 ...`
9. 内容深度和表达方式要匹配该学科的特点和难度级别。

输出要求：
直接返回完整 Markdown，不要加解释。
"""


REVIEWER_PROMPT = """\
你是一位严格的教学质检官。请检查下面这章讲义是否已经达到可发布质量。

学科信息：
{subject_context}

文档内容：
{document}

用户补充要求：
{user_prompt}

原始素材摘要：
{source_summary}

请重点检查：
1. 是否只有一个一级标题，且标题正确。
2. 是否有 `> 📌 本章概要：...` 开头段。
3. 结构是否清晰，章节层次是否自然。
4. 是否遗漏原始素材中的关键概念、关键公式或典型方法。
5. 公式是否明显错误或表达不清。
6. 结尾是否有 `📊 本章标签：...`。
7. 内容深度是否匹配该学科的特点，专业术语使用是否准确。

输出要求：
返回严格 JSON：
{{
  "passed": true,
  "issues": ["问题1", "问题2"],
  "suggestions": ["建议1", "建议2"]
}}
"""


METADATA_PROMPT = """\
请为下面这章知识文档提取元数据。

文档内容：
{document}

要求：
1. 生成一段不超过 50 字的导读摘要 summary。
2. 提取 3-5 个标签 tags，每个标签以 `#` 开头。

输出要求：
返回严格 JSON：
{{
  "summary": "导读摘要",
  "tags": ["#标签1", "#标签2", "#标签3"]
}}
"""
