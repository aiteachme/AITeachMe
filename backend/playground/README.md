# Playground

这里放手动验证脚本，不走 `pytest`，也不写断言。

使用方式：

1. 把测试文件放到 `playground/inputs/`
2. 运行对应脚本
3. 到 `playground/outputs/` 查看输出结果

示例：

- `python playground/try_pdf_parse.py`
- `python playground/try_chunker.py`
- `python playground/try_exam_generate.py`
