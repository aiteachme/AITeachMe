name: web_search
description: 搜索互联网获取最新相关信息，适用于知识库中没有覆盖的内容
version: "1.0"
category: 检索
enabled: true
parameters:
  query:
    type: string
    description: 搜索查询（自然语言）
    required: true
  top_k:
    type: integer
    description: 返回结果数量
    default: 5
