import { AiInteractionWindow } from "../components/interaction";

export function GlobalAssistantPage() {
  return (
    <AiInteractionWindow
      variant="fullscreen"
      className="min-h-[calc(100dvh-7rem)]"
    />
  );
}
