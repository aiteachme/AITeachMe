export const MOCK_DOCUMENT_TITLE = "线性代数基础 —— 从向量到矩阵乘法";

export const MOCK_DOCUMENT_SUMMARY =
  "这是一份用于调试阅读体验的默认样章，用来验证知识文档页面在公式、Mermaid、表格、代码块和长篇段落场景下的排版质量。";

export const MOCK_DOCUMENT_CHAPTERS = [
  "核心概念引入：什么是向量？",
  "矩阵乘法：空间的变换",
  "特征值与特征向量",
  "学习总结与复盘",
];

export const MOCK_DOCUMENT_MARKDOWN = `# 线性代数基础 —— 从向量到矩阵乘法

> **本章导读：** 线性代数是现代机器学习的基石。在神经网络中，所有的数据和权重都以矩阵形式体现，所有的运算最终都归结为矩阵的加法和乘法。学习本章不仅是为了应付考试，更是为了获得理解复杂AI模型的直觉。

---

## 1. 核心概念引入：什么是向量？

在物理学中，向量是具有**大小**和**方向**的箭头；在计算机科学中，向量是一个数字列表。对于我们要学习的深度学习而言，这两者是统一的。

### 1.1 标量、向量、矩阵与张量

- **标量 (Scalar)**: 单个数字，如 $x = 5$ 
- **向量 (Vector)**: 一维数组，表示空间中的一个点或方向，如 $\\vec{v} = \\begin{bmatrix} 1 \\\\ 2 \\end{bmatrix}$
- **矩阵 (Matrix)**: 二维数组，可以视为向量的集合，常用于表达空间的线性变换
- **张量 (Tensor)**: 多维数组，是标量、向量和矩阵在更高维度的推广

> 💡 **思考：** 为什么我们需要张量？一幅彩色图片就是一个三维的大张量 $(H \\times W \\times C)$，其中 $C$ 是 RGB 三通道。

---

## 2. 矩阵乘法：空间的变换

矩阵乘法可能是机器学习中最频繁进行的操作，前向传播 $\\mathbf{Z} = \\mathbf{W}\\mathbf{X} + \\mathbf{b}$ 核心便依赖于它。

**基本规则：** 若 $\\mathbf{A}$ 为 $m \\times n$ 矩阵， $\\mathbf{B}$ 为 $n \\times p$ 矩阵，则它们的乘积 $\\mathbf{C} = \\mathbf{A}\\mathbf{B}$ 是一个 $m \\times p$ 的矩阵。

$$
C_{ij} = \\sum_{k=1}^n A_{ik} B_{kj}
$$

### 2.1 高频考点：维度匹配

> ⚠️ **易错提醒**：在编写 PyTorch 或 NumPy 代码时，90% 的 \`RuntimeError\` 是形状不匹配问题！永远记住：左边矩阵的列数必须等于右边矩阵的行数！

\`\`\`python
import numpy as np

# A 是 2行3列
A = np.array([[1, 2, 3], 
              [4, 5, 6]])

# B 是 3行2列
B = np.array([[7, 8], 
              [9, 10], 
              [11, 12]])

# 矩阵乘法: (2,3) x (3,2) -> (2,2)
C = np.dot(A, B) 
print(C.shape) # 输出 (2, 2)
\`\`\`

---

## 3. 核心抓手：特征值与特征向量

这是系统学习和考试的重中之重。特征向量的意义在于：经过特定矩阵 $\\mathbf{A}$ 变换后，方向不变的向量。

$$
\\mathbf{A}\\vec{v} = \\lambda \\vec{v}
$$

这里 $\\vec{v}$ 就是特征向量，$\\lambda$ 就是特征值。它意味着这个变换在这个特定方向上，仅仅起到了缩放作用，而没有旋转。

### 3.1 概念知识网

\`\`\`mermaid
flowchart LR
    A[方阵 A] --> B(特征多项式)
    B -->|求根| C[特征值 λ]
    C -->|代入| D[齐次线性方程组]
    D -->|求解| E[特征向量 v]
    E -->|拼接组成| F[相似对角化矩阵 P]
    
    style A fill:#3370FF,color:#fff
    style C fill:#FF3B30,color:#fff
    style E fill:#34C759,color:#fff
\`\`\`

---

## 4. 学习总结与复盘

### 总结对照表

| 概念 | 几何意义 | 代数意义 | 在 ML 中的应用 |
| :--- | :---: | :---: | :--- |
| **点积** | 表示两个向量的相似度、投影 | 对应元素相乘求和 | 注意力机制 (Attention) 中计算 Query 与 Key 相似度 |
| **范数** | 向量的长度 | 元素的绝对值衡量 ($L_1, L_2$) | 正则化 (Regularization) 防止过拟合 |
| **逆矩阵** | 撤销原空间的线性变换 | $\\mathbf{A}^{-1}\\mathbf{A} = \\mathbf{I}$ | 解线性方程组、正规方程推导 |

### 本章最后复盘
- [ ] 确保能够手算一个小矩阵的乘法
- [ ] 确保遇到代码抛出 \`mat1 and mat2 shapes cannot be multiplied\` 时知道排查前后的 shape 形状
- [ ] 能够流畅默写并解释特征方程 $\\det(\\mathbf{A} - \\lambda\\mathbf{I}) = 0$
`;
