
export function getMockDocumentMarkdown() {
  return `# 线性代数基础 —— 从向量到矩阵乘法

> **本章导读：** 线性代数是现代机器学习的基石。在神经网络中，所有的数据和权重都以矩阵形式体现，所有的运算最终都归结为矩阵的加法和乘法。学习本章不仅是为了应付考试，更是为了获得理解复杂AI模型的直觉。当我们深入到 Transformer 或者扩散模型等架构的最核心逻辑时，矩阵操作才是真正的主角。

---

## 1. 核心概念引入：什么是向量？

在物理学中，向量是具有**大小**和**方向**的箭头；在计算机科学中，向量是一个数字列表。对于我们要学习的深度学习而言，这两者是统一的。我们从几何直觉出发，构建代数体系。

### 1.1 标量、向量、矩阵与张量

我们可以将数据的维度视作阶梯：
- **标量 (Scalar)**: 单个数字，如 $x = 5$，代表温度或标价。
- **向量 (Vector)**: 一维数组，表示空间中的一个点或方向，如 $\\vec{v} = \\begin{bmatrix} 1 \\\\ 2 \\end{bmatrix}$。在自然语言处理中，它通常用于表示一个词的语义（Embedding）。
- **矩阵 (Matrix)**: 二维数组，可以视为向量的集合，常用于表达空间的**线性变换**。
- **张量 (Tensor)**: 多维数组，是标量、向量和矩阵在更高维度的推广。

> 💡 **思考提示：** 为什么我们需要张量？一幅彩色图片就是一个三维的大张量 $(H \\times W \\times C)$，其中 $C$ 是 RGB 三通道。如果我们处理的是视频（帧序列），那就是一个四维张量 $(T \\times H \\times W \\times C)$。

---

## 2. 矩阵乘法：空间的变换

矩阵乘法可能是机器学习中最频繁进行的操作，前向传播 $\\mathbf{Z} = \\mathbf{W}\\mathbf{X} + \\mathbf{b}$ 的核心计算便依赖于它。将左乘矩阵想象为对空间坐标系进行的“挤压和旋转”。

**基本规则：** 若 $\\mathbf{A}$ 为 $m \\times n$ 矩阵， $\\mathbf{B}$ 为 $n \\times p$ 矩阵，则它们的乘积 $\\mathbf{C} = \\mathbf{A}\\mathbf{B}$ 是一个 $m \\times p$ 的矩阵。

$$
C_{ij} = \\sum_{k=1}^n A_{ik} B_{kj}
$$

### 2.1 高频考点：维度匹配

> ⚠️ **易错提醒**：在编写 PyTorch 或 NumPy 代码时，90% 的 \`RuntimeError\` 都来源于张量的形状（Shape）不匹配问题！**永远记住**：左边矩阵的列数必须等于右边矩阵的行数！

\`\`\`python
import numpy as np
import torch

# A 是 2行3列的矩阵
A = np.array([[1, 2, 3], 
              [4, 5, 6]])

# B 是 3行2列的矩阵
B = np.array([[7, 8], 
              [9, 10], 
              [11, 12]])

# 矩阵乘法: (2,3) x (3,2) -> (2,2)
C = np.dot(A, B) 
print(f"Result shape: {C.shape}") # 输出 (2, 2)

# 在 PyTorch 中，你可以愉快地使用 @ 运算符
tensor_a = torch.tensor(A)
tensor_b = torch.tensor(B)
tensor_c = tensor_a @ tensor_b
\`\`\`

---

## 3. 核心抓手：特征值与特征向量

这是系统学习和考试的重中之重。特征向量的物理意义在于：经过特定矩阵 $\\mathbf{A}$ 的空间拉伸变换后，**方向没有发生改变**的特殊向量。

$$
\\mathbf{A}\\vec{v} = \\lambda \\vec{v}
$$

这里 $\\vec{v}$ 就是特征向量，而 $\\lambda$ 就是对应的特征值。它意味着经过矩阵 $\\mathbf{A}$ 变换后，向量在这个特定方向上的变化仅仅是大小发生了缩放，而没有产生旋转偏移。特征向量构成了理解矩阵的“骨架”。

!tip 做题时先看矩阵维度和变换对象，再去判断该用乘法、特征分解还是几何解释。

:::note
如果你正在从线代过渡到机器学习，这一章最值得反复看的部分其实是“矩阵乘法”和“特征向量的几何含义”。
:::

### 3.1 概念连线图谱

\`\`\`mermaid
flowchart LR
    A[方阵 A] --> B(特征多项式)
    B -->|求根| C[特征值 λ]
    C -->|代入| D[齐次线性方程组]
    D -->|求解| E[特征向量 v]
    E -->|拼接组成| F[相似对角化矩阵 P]
    
    style A fill:#3b82f6,color:#fff
    style C fill:#ef4444,color:#fff
    style E fill:#10b981,color:#fff
    style F fill:#8b5cf6,color:#fff
\`\`\`

---

## 4. 学习总结与复盘

为了加深记忆，我们为大家将常用概念映射到了具体的深度学习场景中。

### 总结对照表

| 概念 | 几何/物理意义 | 代数意义/公式 | 在机器学习中的深度应用 |
| :--- | :--- | :--- | :--- |
| **点积** | 表示两个向量的相似度和相对投影 | $a \\cdot b = \\sum a_i b_i$ | 注意力机制 (Attention) 中计算 Query 与 Key 之间的相关性得分 |
| **范数** | 衡量向量的具体物理长度 | 元素的绝对数值累加 ($L_1, L_2$) | 正则化 (Regularization) 惩罚大权重以防止过拟合 |
| **逆矩阵** | 撤销原空间的线性变换，恢复初始状态 | $\\mathbf{A}^{-1}\\mathbf{A} = \\mathbf{I}$ | 求解联立线性方程组、多元正规方程组的理论推导 |
| **转置** | 对于实数方阵来说沿主对角线翻转 | $(A^T)_{ij} = A_{ji}$ | 处理梯度的后向传播，保持维度校验合法 |

### 本章最后通关清单

- [ ] 我已经彻底理解标量、向量、矩阵、张量的维度递进逻辑。
- [ ] 我能够独立手算一个 $(2 \\times 3)$ 与 $(3 \\times 2)$ 矩阵的乘法。
- [ ] 遇到代码抛出 \`mat1 and mat2 shapes cannot be multiplied\` 时，我知道去排查 \`shape\` 和对齐维度。
- [ ] 能够流畅默写并向他人解释特征方程 $\\det(\\mathbf{A} - \\lambda\\mathbf{I}) = 0$ 的原理。

> *“学习数学不是为了背诵公式，而是通过练习获得描述现实的优雅直觉。”* —— 祝你在接下来的学习中充满信心！`;
}

export const MOCK_DOCUMENT_TITLE = "线性代数基础 —— 从向量到矩阵乘法";

export const MOCK_DOCUMENT_SUMMARY =
  "这是一份用于调试排版阅读体验的默认样章。本章系统性梳理了知识框架，并对Markdown中涉及到的多种渲染语法进行了覆盖测试（含公式、表格、代码、引用、Mermaid）。";

export const MOCK_DOCUMENT_CHAPTERS = [
  "核心概念引入：什么是向量？",
  "矩阵乘法：空间的变换",
  "特征值与特征向量",
  "学习总结与复盘",
];

export const MOCK_DOCUMENT_MARKDOWN = getMockDocumentMarkdown();
