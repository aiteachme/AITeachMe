/**
 * Generated by orval v8.8.0 🍺
 * Do not edit manually.
 * AITeachMe
 * 本地优先的 AI 助教后端服务。
 * OpenAPI spec version: 0.0.3-beta.1
 */

/**
 * Stable graph-build metrics used by docs and graph progress UIs.
 */
export interface KnowledgeGraphBuildMetricsResponse {
  /** Legacy parsed-file chunk count; docs-sync builds keep this at 0. */
  processed_chunks?: number;
  /** Knowledge-doc sections analyzed by kg_doc_sync. */
  doc_sync_section_count?: number;
  /** Knowledge units created or updated by docs-sync. */
  doc_sync_unit_changes?: number;
  /** Graph edges created or updated by docs-sync. */
  doc_sync_edge_changes?: number;
  /** Elapsed milliseconds for the latest graph-sync step. */
  elapsed_ms?: number;
  /** Knowledge graph revision number produced by the latest docs-sync. */
  revision_no?: number;
  /** Knowledge document version number last synced into the graph. */
  last_synced_doc_version_no?: number;
  /** Source of the knowledge document markdown used for docs-sync. */
  knowledge_doc_source?: string | null;
  /** Knowledge document chapter count seen by docs-sync. */
  knowledge_doc_chapter_count?: number;
  /** Structured provenance refs written by the latest graph sync. */
  source_ref_count?: number;
  /** Knowledge units seeded from DocGen document_backbone; kept for compatibility, currently should be 0. */
  backbone_unit_count?: number;
  /** Knowledge edges added from DocGen document_backbone between already extracted units. */
  backbone_edge_count?: number;
  /** No-LLM relation-stitching edges added before graph persistence. */
  stitched_edge_count?: number;
  /** Edges stitched inside the same section to reduce isolated units. */
  section_local_stitch_edge_count?: number;
  /** Edges stitched from explicit cross-unit mentions in extracted text. */
  mention_stitch_edge_count?: number;
  /** Extracted units with zero incident edges after relation stitching. */
  graph_isolated_unit_count?: number;
  /** Undirected connected component count after relation stitching. */
  graph_component_count?: number;
  /** Largest connected component size after relation stitching. */
  graph_largest_component_unit_count?: number;
  /** Extracted active-unit count used for graph health metrics. */
  graph_active_unit_count?: number;
  /** Extracted active-edge count used for graph health metrics. */
  graph_active_edge_count?: number;
  /** Average undirected degree after relation stitching. */
  graph_avg_degree?: number;
  /** Percentage of extracted units with no incident edge after relation stitching. */
  graph_isolated_unit_pct?: number;
  /** Stable graph anchors seen during the latest docs-sync. */
  stable_anchor_count?: number;
  /** Knowledge units deprecated by the latest docs-sync. */
  deprecated_unit_count?: number;
  /** Knowledge edges deprecated by the latest docs-sync. */
  deprecated_edge_count?: number;
  /** DocGen-side KG prefetch status for the latest auto sync. */
  prefetch_status?: string | null;
  /** Section payloads produced by the DocGen-side KG prefetch. */
  prefetch_section_count?: number;
  /** Prefetched section payloads reused by final graph sync. */
  prefetch_reused_section_count?: number;
  /** Final sections that required normal catch-up extraction. */
  prefetch_catchup_section_count?: number;
  /** Prefetched section payloads discarded because final content changed. */
  prefetch_stale_section_count?: number;
  /** Prefetch section payloads that failed before final graph sync. */
  prefetch_failed_section_count?: number;
}
