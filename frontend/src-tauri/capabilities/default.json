{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Enables default permissions for the main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
